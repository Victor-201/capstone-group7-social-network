import React from 'react';
import './style.scss';

export const SearchPopup = ({ user, onClose }) => {
    return (
        <div className="popup__search">
        </div>
    );
};

export default SearchPopup
